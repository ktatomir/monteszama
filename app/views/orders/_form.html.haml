= form_for order, html: {class: 'form-horizontal' } do |f|
	= f.hidden_field :owner_id, value: github_user.id
	= f.text_field :desc, placeholder: "describe new order"
	= f.submit "Create an order", class: "btn btn-primary"